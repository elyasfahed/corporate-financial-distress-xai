# ============================================================
# Thesis: Explainable AI for Corporate Financial Distress Prediction
# Python 3.11 | PyCharm | Miniconda
# Install: pip install -r requirements.txt
#
# REPRODUCIBILITY (2026-07-12 audit fix): the ranges below express
# intent only. The exact environment the results were produced with is
# pinned in requirements-lock.txt (pip freeze of the project .venv,
# Python 3.11.9). To reproduce: pip install -r requirements-lock.txt.
# Always run pipeline scripts with ./.venv/Scripts/python.exe — NOT the
# system Python (a stage-d run under Python 3.13 triggered this note).
# ============================================================

# ── Core data ───────────────────────────────────────────────
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyarrow>=14.0          # Parquet support for pandas
jinja2>=3.1            # Required by pandas DataFrame.to_latex

# ── WRDS data access ────────────────────────────────────────
wrds>=3.1.3            # WRDS Python API (requires ~/.pgpass credentials)

# ── Machine learning ────────────────────────────────────────
scikit-learn>=1.4
xgboost>=2.0

# ── Hyperparameter tuning ───────────────────────────────────
optuna>=3.5            # Bayesian optimisation (Pre-Specified Empirical Design §9.3)

# ── Explainability ──────────────────────────────────────────
shap>=0.44             # SHAP TreeExplainer for XGBoost/RF
lime>=0.2              # LIME supplementary XAI cross-check (Stage 14)

# ── Class imbalance (RC3 only) ──────────────────────────────
imbalanced-learn>=0.12 # SMOTE (robustness check RC3 only)

# ── Statistics ──────────────────────────────────────────────
statsmodels>=0.14

# ── Visualisation ───────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13

# ── Serialisation ───────────────────────────────────────────
joblib>=1.3            # Model saving/loading
pyyaml>=6.0            # Config files

# ── Utilities ───────────────────────────────────────────────
tqdm>=4.66             # Progress bars
python-docx>=1.1       # Read Word-format design documentation
pytest>=8.0            # Regression tests for statistical routines
